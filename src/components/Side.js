import React from 'react'

const Side = (props) => {
    //use react router to navigate to a favorites page 
    // maybe a statistics page 

    return(
        <div className="side_bar">
            <ul>
                <li>Favorites</li>
                <li>Galleries</li>
            </ul>

        </div>
    )
}

export default Side